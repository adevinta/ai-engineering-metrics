apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-collector
  labels:
    app.kubernetes.io/name: ai-collector
  annotations:
    # Add AWS IAM role annotation if using IRSA
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/ai-reporter-role
automountServiceAccountToken: true